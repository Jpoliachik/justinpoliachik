// __tests__/index.test.jsx

import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

describe("About", () => {
  it("renders the correct information", () => {
    expect(true);
  });
  it("renders the menu item on desktop", () => {
    expect(true);
  });
  it("renders the menu item on mobile screen width", () => {
    expect(true);
  });
  it("renders other stuff", () => {
    expect(true);
  });
});
