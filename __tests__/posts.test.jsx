// __tests__/index.test.jsx

import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

describe("Posts", () => {
  it("renders the latest post first", () => {
    expect(true);
  });
  it("renders pagination link when list is too long", () => {
    expect(true);
  });
  it("renders no pagination link when list is short", () => {
    expect(true);
  });
  it("renders empty state", () => {
    expect(true);
  });
  it("renders loading state while waiting on data fetch", () => {
    expect(true);
  });
});
